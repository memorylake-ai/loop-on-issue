---
description: Set up (or reconfigure) the optional DingTalk bot — questions, reports, and requirement intake
argument-hint: "[--disable] [any preferences]"
---

Invoke the `dingtalk-bot` skill and set up the chat bot for me.

$ARGUMENTS

Start by telling me what is already configured, so we only do the missing parts.
The DingTalk console steps I have to do myself — hand those over clearly rather
than trying to do them for me, and be explicit about the two that are usually got
wrong: Stream mode rather than HTTP, and publishing the app.

Never print my credentials back to me, and never put them anywhere inside a
repository.
