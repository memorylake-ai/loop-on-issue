---
description: Decompose a requirement into queue-ready issues the swarm can execute
argument-hint: "<requirement, spec path, or epic issue number>"
---

Invoke the `loop-issue-creator` skill and decompose this into issues:

$ARGUMENTS

Read the actual code before writing any slice — I want the delta, not a restatement
of the request, with real `file:line` references. Draft every slice in full and
wait for my approval before creating anything.
