<!-- loop-on-issue:template -->

### 背景

为什么现在要做，以及读者需要知道什么才不会误读下面的内容。一小段即可。

### 现状与期望

今天实际发生的行为（附上真正读过代码后得到的 `file:line`），对比应该发生的行为。
写清具体位置的切片，session 能立刻动手；只是复述需求的切片，session 得先把这份
调研重做一遍。

### 验收标准

1. 可核对的陈述，让 session 不必发问就能判断做完没有。
2. 足够穷尽，「这些全部成立」就等于完成。

### 文件边界

这个切片拥有的文件。同时点名*兄弟切片*拥有的文件，避免另一个 worktree 里并发运行的
session 闯进去。

### 明确不做

session 有理由顺手扩进来、但这次不做的部分。这是把切片控制在一次会话内的关键。

### 验证

可以原样执行的真实命令：

```sh
# 例如 npm test -- path/to/suite
```
